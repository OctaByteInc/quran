syntax = "proto3";

import "shared.proto";
import "entity.proto";

package quran;

service ImageSvc {
    rpc CreateImage (Image) returns (Image) {}
    rpc FindImageByAyahId (IDRequest) returns (Image) {}
}