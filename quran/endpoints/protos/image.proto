syntax = "proto3";

import "shared.proto";

package quran;

message ImageEntity {
    string ayah_id = 1;
    string image = 2;
}

service Image {
    rpc CreateImage (ImageEntity) returns (ImageEntity) {}
    rpc FindImageByAyahId (AyahId) returns (ImageEntity) {}
}